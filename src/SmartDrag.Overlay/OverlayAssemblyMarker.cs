namespace SmartDrag.Overlay;

/// <summary>
/// Marker for the future production overlay assembly.
/// The rendering stack is deliberately not selected until the raw Win32 P0 proof passes.
/// </summary>
public sealed class OverlayAssemblyMarker;
