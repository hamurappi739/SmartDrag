# ADR-010 — Single overlay HWND with internal hit testing

**Status:** Accepted for proof

## Decision

Use one HWND and hit-test action rows from DragOver coordinates. Avoid one HWND per action until evidence requires it.
