# ADR-006 — IDropTarget is intentional action target

**Status:** Accepted

## Decision

Register only the compact SmartDrag overlay as an OLE drop target. Do not use full-screen/invisible targets to spy on arbitrary drags.
