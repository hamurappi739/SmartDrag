# ADR-002 — Domain core independent from Windows/UI

**Status:** Accepted

## Decision

SmartDrag.Core exposes domain models, policies, contracts, and pure state transitions only. No HWND, WPF/WinUI, codec types, or concrete filesystem implementation.
